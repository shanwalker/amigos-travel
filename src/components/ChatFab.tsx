import { useEffect } from 'react';

export const ChatFab = () => {
  // Tawk.to script is now injected globally via index.html to ensure it appears on all pages,
  // including the Trip Proposal Viewer.
  // This component is kept to avoid breaking imports in Index.tsx and IndexMain.tsx.
  return null;
};
